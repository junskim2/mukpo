<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">

<!-- 회원가입 sql -->
<insert id="userInsert" parameterType="user.domain.userVO"> 
	INSERT INTO member VALUES(#{userId}, #{userPass}, #{userName}, #{userTel}, #{userBorn},#{userEmail},)	
</insert>

<!-- 로그인 sql -->
<select id="userLogin" parameterType="user.domain.userVO" resultType="user.domain.userVO"> 
	
	select * from member where m_id=#{userId}
	<if test="userPass !=null">
		AND m_pw=#{userPass}
	</if>
</select>
</mapper>